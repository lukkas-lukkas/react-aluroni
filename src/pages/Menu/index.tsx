import { ReactComponent as Logo } from "../../assets/logo.svg";

export default function Menu() {
    return (
        <main>
            <nav className="menu">
                <Logo />
            </nav>
        </main>
    );
}
